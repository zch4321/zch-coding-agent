协调并完成这个 Swarm 目标：${objective}

条件允许时，先使用父 Agent 的命令工具执行相关验证，再调用 `swarm_run`。把命令、退出码和精简后的关键输出放入 `sharedContext`；如果无法运行验证，也要在其中明确说明。子 Agent 只读，不能执行命令、构建或测试。

把公共背景、证据、约束和输出要求放入 `sharedContext`，每项 task 只描述当前 Child 的具体职责。两部分合起来必须自包含，因为子 Agent 不会收到父对话历史。

每个 assistant turn 优先只调用一次 `swarm_run`。同一 Run 下的多个 Swarm Job 会严格按顺序执行，并且可能耗时较长。适合独立交叉验证时，使用接近当前上限的 Agent 数量，也可以把同一任务交给多个 Agent。分配器会优先轮换合格模型，但模型池不足时仍可能复用同一模型。为每项任务选择足够使用的最低能力等级。

每个副本的结果会分别返回。请自行比较结果、解决分歧并给出最终综合结论。
