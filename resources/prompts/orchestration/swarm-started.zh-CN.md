协调并完成这个 Swarm 目标：${objective}

使用 `swarm_run` 将相互独立、只读的调查任务委派给模型池。子 Agent 不会收到父对话历史，因此每个任务都必须包含完成任务所需的全部上下文。

每个 assistant turn 优先只调用一次 `swarm_run`。同一 Run 下的多个 Swarm Job 会严格按顺序执行，并且可能耗时较长。默认使用 `agentCount: 1`，仅在需要独立交叉验证时增加副本数量，并为每个任务选择足够使用的最低能力等级。

每个副本的结果会分别返回。请自行比较结果、解决分歧并给出最终综合结论。
