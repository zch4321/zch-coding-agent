请生成一份将替代旧对话历史的可追溯压缩摘要。

要求：
- 保留用户目标、关键决定、已运行工具、文件变更、未完成事项和风险。
- 这份摘要会作为 `<compact_history>` 用户消息重新注入，并替换旧的 provider messages；不要假设旧历史仍可见。
- 只输出摘要正文，不要调用工具，不要声称已经修改文件。
- 标注不确定信息，避免把推断写成事实。
- 输出结构化 Markdown，便于后续继续对话。
