{
  "schemaVersion": 1,
  "caseId": "no-change-contract",
  "suiteId": "core-harness-8",
  "suiteRevision": "v1",
  "checks": [
    {
      "id": "traversal",
      "acceptanceGroupId": "traversal",
      "command": {
        "executable": "node",
        "args": [
          "--input-type=module",
          "-e",
          "import assert from 'node:assert/strict'; import { isSafeRelativePath } from './src/safe-path.mjs'; assert.equal(isSafeRelativePath('../secret'), false); assert.equal(isSafeRelativePath('src/../../secret'), false); assert.equal(isSafeRelativePath('src\\\\..\\\\secret'), false)"
        ],
        "timeoutMs": 5000,
        "maxOutputBytes": 65536
      }
    },
    {
      "id": "absolute-paths",
      "acceptanceGroupId": "absolute-paths",
      "command": {
        "executable": "node",
        "args": [
          "--input-type=module",
          "-e",
          "import assert from 'node:assert/strict'; import { isSafeRelativePath } from './src/safe-path.mjs'; assert.equal(isSafeRelativePath('/etc/passwd'), false); assert.equal(isSafeRelativePath('C:\\\\temp\\\\file.txt'), false); assert.equal(isSafeRelativePath(''), false)"
        ],
        "timeoutMs": 5000,
        "maxOutputBytes": 65536
      }
    }
  ],
  "oracle": { "kind": "no-change" },
  "mutants": [
    {
      "id": "leading-traversal-only",
      "description": "Rejects only leading traversal and misses embedded or Windows traversal.",
      "patch": "diff --git a/src/safe-path.mjs b/src/safe-path.mjs\n--- a/src/safe-path.mjs\n+++ b/src/safe-path.mjs\n@@ -1,7 +1,4 @@\n export function isSafeRelativePath(value) {\n   if (typeof value !== 'string' || value.length === 0) return false\n-  const normalized = value.replaceAll('\\\\', '/')\n-  if (normalized.startsWith('/') || /^[A-Za-z]:\\//.test(normalized)) return false\n-  return !normalized.split('/').includes('..')\n+  return !value.startsWith('../') && !value.startsWith('/')\n }\n",
      "expectedFailedGroups": ["traversal", "absolute-paths"]
    },
    {
      "id": "substring-check",
      "description": "Rejects dot-dot substrings but permits absolute paths.",
      "patch": "diff --git a/src/safe-path.mjs b/src/safe-path.mjs\n--- a/src/safe-path.mjs\n+++ b/src/safe-path.mjs\n@@ -1,7 +1,4 @@\n export function isSafeRelativePath(value) {\n   if (typeof value !== 'string' || value.length === 0) return false\n-  const normalized = value.replaceAll('\\\\', '/')\n-  if (normalized.startsWith('/') || /^[A-Za-z]:\\//.test(normalized)) return false\n-  return !normalized.split('/').includes('..')\n+  return !value.includes('..')\n }\n",
      "expectedFailedGroups": ["absolute-paths"]
    }
  ]
}
